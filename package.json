{
  "name": "workshop-embedding-api",
  "version": "1.0.0",
  "description": "Serverless embedding API for MongoDB AI workshop",
  "main": "api/embed.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}